package main

import "fmt"

func main() {
	var a int = 5
	// fmt.Println(a++) "u" we can't use incre and decr as a expression only golang allow us to statement,statement also we can't pre-incre only allow post-incre and dec
	fmt.Println()
}
